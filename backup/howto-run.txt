docker compose up









# ssh into the pg-client container and input a million of data


docker exec -it pg-client bash

psql -h postgres -U admin -d testdb


CREATE TABLE IF NOT EXISTS t1 (
          id SERIAL PRIMARY KEY,
          name TEXT,
          created_at TIMESTAMP DEFAULT NOW()
        );



INSERT INTO t1 (name) SELECT 'user_' || generate_series(1, 100);



select count(*) from t1;

select * from t1;












docker exec -it pg-server bash


psql -h localhost -U admin -d testdb





# backup the database

docker exec -it pg-server bash

su - postgres
cd data
pg_dump -Fc -U admin -d testdb -f backup_file.dump




or


pg_dumpall -U admin > full_cluster_backup.sql













# restore the database


Create an empty database on your PostgreSQL server where the data will be restored.



createdb -U admin new_dbname

pg_restore -Fc -U admin -d new_dbname backup_file.dump

psql -h localhost -U admin -d new_dbname -c "select count(*) from test;"
psql -h localhost -U admin -d new_dbname -c "select count(*) from test"

psql -h localhost -U admin -d testdb





or

psql -U admin -d testdb -f full_cluster_backup.sql


